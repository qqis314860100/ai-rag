Done.

I rebuilt `web/src/components/chat/DocPreview.tsx` into a unified preview shell with text, Markdown, and raw views, plus clearly disabled PDF, HTML, and code tabs. It now resets state cleanly when the source changes, and the raw view shows an empty state when the backend returns no original content.

Verification passed:
- `pnpm run lint:web`
- `pnpm run build:web`
- Chrome smoke on `http://localhost:5173/chat`: opened evidence preview, switched between supported modes, confirmed disabled future-format tabs, and confirmed the raw empty state

Commits:
- `c3022bd` `feat(web): add unified document preview shell`
- progress log updated in the following chore commit
