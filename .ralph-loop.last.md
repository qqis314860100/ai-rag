Done.

`web` now scopes retry to the clicked answer, routes user edit through the existing delete/truncate flow before re-asking, disables edit/delete on unsaved temp user messages, and reloads persisted messages after a stream completes so later actions target backend IDs.

Verification:
- `pnpm run lint:web` passed with the repo’s existing warnings only.
- `pnpm run build:web` passed.
- Chrome smoke on `http://localhost:5174/chat`: verified answer actions exclude delete, user actions include copy/edit/delete, edit mode opens and cancels, delete opens the expected confirmation and was canceled.

Commits:
- `ff85043` `fix(web): tighten chat message actions`
- `81b5652` `chore: update ralph progress`

`progress.txt` is updated and committed in the root-level chore commit. Blocker note: root `pnpm dev` is currently miswired in this repo, so I used `pnpm run dev:web` for the smoke check.