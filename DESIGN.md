# AI-RAG 设计上下文

## Design Register

产品型 UI。设计服务任务完成，不做品牌展示页。默认采用克制、温和、可信的企业工具风格。

## Visual Direction

- 基调：温和企业、低噪声、高可读、证据可信。
- 场景：工程师在办公室、会议室或产线附近使用笔记本查询资料，需要快速扫读和准确操作。
- 画面感：干净的白色内容面、温暖中性色、少量金色强调、明确的信息层级。
- 记忆点：回答和证据组织得清楚，图解像工作白板而不是装饰海报。

## Existing System

- 样式入口：`web/src/styles/tokens.css` 和 `web/src/styles/index.css`。
- 主要前端区域：`web/src/features/chat/`。
- 通用组件：`web/src/components/ui/`。
- 图解渲染：`@excalidraw/excalidraw`，前端应尽量纯渲染后端返回的 DiagramIR / scene。

## Color

- 默认使用现有设计 token，不为单个页面另起独立配色体系。
- 主色和文字以温暖 stone / neutral 为主，强调色少量使用 warm gold。
- 语义色只表达状态：成功、警告、危险、信息，不做装饰。
- 不使用大面积紫蓝渐变、霓虹发光、重玻璃拟态或高饱和背景。

## Typography

- 产品 UI 优先使用稳定、可读的 sans 字体栈。
- 中文界面重视行高和字重层级，不用夸张 display 字体做按钮、标签或数据。
- 正文行长控制在舒适范围，代码、表格和引用可以更密。
- 不使用随视口变化的字体缩放。

## Layout

- 聊天页主阅读区优先，右侧栏只承载导航和知识笔记。
- Artifact 应跟随回答出现，详情在弹窗中查看，不挤压聊天阅读区。
- 避免卡片套卡片；页面区块不做浮动大卡片，卡片只用于独立条目、弹窗和可重复项目。
- 常用操作靠近内容结束位置，少用远离上下文的独立工具区。
- 文本、按钮、节点和图表标签不能溢出或遮挡。

## Components

- 按钮：常用命令用文字或图标+文字；工具性动作优先 lucide 图标并提供 title。
- Badge：只表达状态、证据数量、可信度或类型，不用作装饰。
- Modal：用于图解、文档预览、确认等明确聚焦任务；不要把普通信息默认塞进弹窗。
- EmptyState：说明当前缺什么、下一步可做什么，不写泛泛的营销文案。
- 图解节点：稳定尺寸、清晰文本、线条不穿核心文字、父子/流程方向明确。

## Motion

- 动效只服务状态变化，例如加载、展开、定位、轻量反馈。
- 聊天发送、流式、重试、滚动和输入框高度必须稳定，避免闪跳。
- 不做装饰性入场动画，不让用户等待动画完成才能工作。

## AI Slop Guardrails

- 不用星星、魔法棒、发光 orb、紫蓝渐变来表达 AI。
- 不做无意义大圆角卡片网格。
- 不在企业工具里堆 hero、slogan 或营销式分屏布局。
- 不为了“高级感”牺牲扫描效率、对比度和可访问性。
- 图表和图解必须先保证语义正确，再谈视觉质感。

## Impeccable Workflow

- 起稿：`$impeccable craft <目标>`，用于新页面、大组件或显著 UI 改造。
- 审查：`$impeccable critique <目标>`，用于高影响 UI、图解、发布前体验检查。
- 打磨：`$impeccable polish <目标>`，用于间距、层级、状态、文案和组件一致性。
- 优化：`$impeccable optimize <目标>`，用于渲染性能、重组件、懒加载和动画流畅度。
- 适配：`$impeccable adapt <目标>`，用于移动端、普通笔记本、宽屏和弹窗适配。

## Acceptance

- 普通 UI：构建通过，关键状态人工检查或定点截图。
- 高影响 UI：桌面和移动断点截图检查，必要时浏览器 smoke。
- 图解 UI：节点不重叠，线条不穿核心文字，方向语义正确，弹窗可滚动，刷新后 artifact 可恢复。

