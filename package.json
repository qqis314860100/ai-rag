{
  "name": "ai-rag",
  "version": "1.0.0",
  "private": true,
  "description": "企业级电池产线 RAG 知识库系统",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "dev:rag": "cd rag && python -m uvicorn app.main:app --reload --port 8001",
    "dev": "pnpm run --parallel dev:web dev:api",
    "build:web": "pnpm --filter web build",
    "build:api": "pnpm --filter api build",
    "build": "pnpm run build:web && pnpm run build:api",
    "typecheck": "pnpm --filter web build && pnpm --filter api build",
    "lint": "bash scripts/lint.sh",
    "test": "bash api/tests/run-tests.sh",
    "check": "bash scripts/check-harness.sh"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1"
  }
}
