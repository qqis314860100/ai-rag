# 电池产线 RAG 知识库 PRD

> 早期文件名「聊天体验改造 PRD」保留作为文件名以兼容 `scripts/ralph_task_queue.py`；产品范围已演进到企业级 RAG 知识库 + 知识资产沉淀层，标题随之更新。
>
> 与之配套的文档：
> - [项目架构总览](docs/项目架构总览.md)：现状架构盘点与优化建议
> - [企业级文档知识库智能工作台升级方案](docs/企业级文档知识库智能工作台升级方案.md)：产品演进方案
> - [项目架构与边界标准](docs/架构标准.md)：服务边界硬规则
> - [项目执行规则清单](docs/EXECUTION_RULES.md)：执行/验证/提交规则

## 产品愿景

让产线一线工程师、质量、维护、安全、新人在一个对话窗口内，问出工艺、设备、SOP、参数、异常和培训问题，拿到**带证据**的回答和**自动结构化**的图解/表格；让有价值的回答和引用沉淀成可治理的企业知识资产，并反哺下一次检索与回答。

简化为一句话：**问 → 带证据回答 → 出图/出表 → 沉淀 → 复用**。

### 目标用户与场景

| 用户 | 典型场景 | 对回答的要求 |
|------|----------|--------------|
| 工艺工程师 | 参数比对、工艺窗口确认、异常成因排查 | 精准、有引用、能反向定位文档 |
| 产线技术员 | SOP 查询、异常处理流程、设备操作步骤 | 步骤清晰、能出流程图、能离线复用 |
| 质量工程师 | 检测标准、合格范围、不合格品处理 | 标准/参数表、能对比文档差异 |
| 维护与设备 | 故障排查、备件、点检/保养记录 | 故障树/流程图、关联设备文档 |
| 安全管理 | 应急流程、合规检查、危险源识别 | 流程合规、来源权威、不允许编造 |
| 新人/培训 | 岗位 SOP、术语解释、设备介绍 | 解释清楚、图解为主、可沉淀为学习笔记 |

### 价值主张

- 把分散在 PDF/Word/Wiki/纸面 SOP 中的知识统一为可检索资产
- 把工程师之间口头传递的隐性知识沉淀为可治理、可审计的显性知识
- 让低置信、无证据的回答**被拒答**而不是被编造，降低误用风险
- 让高置信回答沉淀为可复用的知识卡 / FAQ，逐步形成企业级知识资产

## 背景

当前项目是企业级电池产线 RAG 系统，核心价值在聊天问答、知识引用、文档预览和后续知识沉淀。前端已经具备会话、流式响应、引用展示、收藏、反馈、草稿和基础布局，但体验仍有几个明显问题：

- 问答卡片动作不够符合语义：用户问题可以编辑/删除，回答应保留复制、反馈、收藏、重试等动作，但不应随便删除。
- 右侧区域更适合作为当前会话导航、章节、证据、笔记和后续结构化输出入口，而不是全量引用仓库,如何设计需头脑风暴一下输出具体设计方案。
- 文档预览需要逐步支持 Markdown、PDF、HTML、代码块等标准格式，并为后续 AI 生成思维导图、流程图预留入口。

## 目标

用 Ralph loop 方式把聊天体验改造拆成小任务执行。AFK 每轮只做一个可验收任务；手工开发不需要套用 Ralph 流水账。提交按单主题组织，同主题可少量跨服务，并按普通/高风险两档选择足够小的验证。

## 成功指标

指标分为产品、质量、效率三类。所有指标默认按周聚合，列出 P50/P95；具体阈值随阶段迭代调整，不写死在文档里。

### 产品指标

| 指标 | 目标方向 | 当前度量来源 |
|------|----------|--------------|
| 周活跃用户 / 周提问数 | 上升 | `browse_history` + `chat_messages` |
| 单会话平均轮次 | 反映粘性，过短或过长都不健康 | `chat_messages` 按 session 聚合 |
| 回答采纳率（👍 ÷ (👍+👎)） | ≥ 0.75 | `feedback` 表 |
| 回答收藏率 | 高频问题应高于均值 | `favorites` |
| 引用打开率（用户点击引用比例） | 反映证据可信度，目标 ≥ 0.4 | 前端 tracking → `audit_logs` |
| 知识沉淀转化率（高置信回答 → 知识卡草稿 → 发布） | ≥ 0.3 | `knowledge_cards` 状态机 |

### 质量指标

| 指标 | 目标 | 度量 |
|------|------|------|
| 检索 Recall@5（在评估集上命中正确文档） | ≥ 0.85 | `rag/evals/` |
| 回答事实一致率（claims 全部能在 citations 中找到支撑） | ≥ 0.9 | LLM as judge + 抽样人工核对 |
| 拒答正确率（低证据/无证据样本拒答比例） | ≥ 0.95 | `rag/evals/refusal/` |
| 误拒率（应该能答却拒答） | ≤ 0.05 | 同上 |
| 图解合理性（节点 ≤ 12、连线无环、与引用一致） | ≥ 0.9 | DiagramIR 校验器 + 抽样 |
| 知识卡发布通过率 | 反映 AI 草稿质量，目标 ≥ 0.6 | 审核记录 |

### 效率指标

| 指标 | 目标 |
|------|------|
| 首 token 延迟 P95 | ≤ 2.5s |
| 完整回答 P95 | ≤ 12s |
| 文档导入耗时（每 MB P95） | ≤ 5s |
| Visual Planner 不阻塞主回答 | 100% 异步 |
| Artifact 生成失败不影响回答展示 | 100% |

## 当前实际状态（2026-05-24）

- `web` 已完成回答作用域证据详情、问答卡片动作语义、发送/中断按钮状态、重试/编辑分支流转，以及发送/重试时标题、输入框、消息列表的稳定性修复。
- 根启动脚本已从错误的 `pnpm run --parallel dev:web dev:api` 调整为 workspace filter 方式；`dev:rag` 已改为使用 `rag/.venv` 内 Python。
- 聊天主流程已完成一次完整端到端回归，覆盖登录、真实 RAG 流式生成、中断、重试、删除确认、收藏/反馈、引用详情、长会话滚动恢复；文档原文预览遇到缺失 raw 内容时会显示既有空态。
- 右侧栏和统一文档预览的壳已经搭好，但真实当前线程目录、近期证据、笔记持久化、结构化输出、PDF、HTML、代码块预览、AI 整理结果仍在等待 API / RAG / 前端同步完成。
- API 与 RAG 侧此前标记为完成的契约/能力，按页面真实能力重新判定为未完成；必须和前端真实联调、验证后才能重新勾选。

最终状态：

- 聊天主区域专注问答阅读和操作。
- 引用从“全局右侧固定列表”调整为“回答作用域内的证据查看”。
- 右侧栏的壳和文档预览的壳都已存在，但真实数据同步、PDF/HTML/代码预览和 AI 整理结果仍未接完。
- 长对话滚动位置在页面切换、会话切换、发送消息等关键路径下稳定可控。
- 问答卡片动作符合用户心智，并且有必要的二次确认。
- 前端、后端、RAG 的源数据能力按服务边界逐步扩展，不伪造尚不存在的数据。

## 非目标

- 不在同一轮同时修改 `web`、`api`、`rag`。
- 不把缺失后端能力伪装成前端已完成能力。
- 不为了 UI 占位新增无真实数据来源的 API。
- 不在普通功能任务里修改根 lockfile、包管理器配置或项目元数据。
- 不把 Ralph 循环跑在脏工作区上，除非用户明确授权。
- 不做实时设备数据接入（MES/PLC/SCADA 实时流），只接收离线导出的文档/报表。
- 不做工作流执行引擎，不替代 OA/审批系统；只做知识沉淀的人审流程。
- 不做多语言界面（仅中文），不做移动端原生 App（仅响应式 Web）。
- 不做通用搜索引擎；只检索企业上传的知识库，不爬取外部网络。
- 不做图片/视频内容理解；图片仅作为 artifact 受控生成或附件展示。
- 不在 RAG 层做用户/权限校验，权限由 `api` 统一守门。
- 不把低置信、无引用或拒答回答沉淀为知识资产。
- 不做面向最终用户的二次开发 SDK；OpenAPI 仅面向受控的企业内系统集成。

## 硬约束

- AFK/Ralph 每轮只选择任务队列中的第一个未完成任务；手工开发只需保持单主题。
- 每个 commit 只表达一个主题；同主题可少量跨 `web`、`api`、`rag`，无关主题必须拆分。
- 每轮完成后必须先完成对应风险验证，再提交。
- Commit 使用 Conventional Commits；单服务优先用 `web`、`api`、`rag` scope，跨服务或仓库级可用 `repo` 或省略 scope。
- Commit message 不包含 AI footer。
- 普通改动只跑目标函数、语法、类型、局部 smoke、相关 build/test 或定点烟雾；不强制全量 build 或浏览器回归。
- 高风险改动才要求真实浏览器/API/RAG 链路验证。
- Ralph 自动循环中，功能提交可以顺带只修改 `PRD.md` checkbox 来推进队列。
- `progress.txt` 只在阶段完成、阻塞、范围变化、用户要求或 AFK loop 结束时同步，不再每轮必交。

## 质量目标

### 检索

- 文档导入必须保留 document/section/chunk/page/offset/format 元数据，使引用可定位到原文位置。
- 检索默认 top-k = 8，重排后保留 top-n = 5；超出阈值需在 PR 描述里给出 evals 对比。
- 同一问题在重复检索时应得到稳定结果，禁止引入不可重放的随机性（除非显式标注）。

### 回答

- 回答必须输出 `AnswerIR`，包含 `answer / claims / citations / confidence / warnings`。
- 每条 claim 必须至少有一条 citation 可追溯；找不到 citation 的 claim 视为质量缺陷。
- 当证据冲突、低置信或低信息输入时返回结构化 `insufficient_context`，不允许编造。
- 拒答文案要给出"缺少哪类资料"和"建议补充什么"，不是只丢一句"无法回答"。

### Artifact

- Visual Planner 默认保守：低置信、无证据、回答过短一律不生成。
- 同一回答最多自动生成 1 个主 artifact；用户显式要求时可追加。
- DiagramIR 必须通过 schema 校验和拓扑检查（节点上限、连线方向、无孤立节点、无环或允许指定方向）。
- 图解失败必须不影响主回答；artifact 状态 `failed` 时前端展示降级。

### 知识资产

- 只有高置信、有引用、未拒答的回答可进入知识卡草稿。
- 草稿到发布必须经过人工审核；发布版本必须保留引用证据。
- 已发布资产反哺 RAG 时，必须在回答 metadata 中标记使用的资产 ID 与版本号。
- 资产删除采用归档（不物理删除），保留审计链。

## 性能预算

| 链路 | 指标 | P50 目标 | P95 目标 | 上限 |
|------|------|----------|----------|------|
| 流式聊天首 token | TTFB | ≤ 1.2s | ≤ 2.5s | 5s 超时降级 |
| 流式聊天完整回答 | 总时长 | ≤ 6s | ≤ 12s | 30s 超时 |
| 非流式聊天 | 总时长 | ≤ 8s | ≤ 15s | 30s 超时 |
| 检索接口 `/rag/search` | 总时长 | ≤ 300ms | ≤ 800ms | 3s 超时 |
| 文档 ingest（每 MB） | 总时长 | ≤ 2s | ≤ 5s | 60s 超时 |
| Artifact 生成 | 总时长 | ≤ 4s | ≤ 10s | 异步，不阻塞回答 |
| 知识卡草稿生成 | 总时长 | 异步 | 异步 | 后台任务，不进请求路径 |

- 超过 P95 必须报警并归档到 `audit_logs` 供复盘
- 任何同步链路引入新的 LLM 调用前必须先评估对 P95 的影响
- 长文档（> 10MB）走分片导入，避免单请求超时

## 安全与权限

### 身份与会话

- 所有 API 经 `extractUser` + `requireAuth` 中间件鉴权（开发模式默认 `system_admin`，生产必须关闭默认）
- 关键写操作（设置变更、用户管理、知识卡发布、文档删除）必须经 `requirePermission`
- 会话与消息按 user_id 隔离；非本人会话的读/写一律 403

### 数据边界

- `web` 不接触原始向量、不接触模型 API key、不直连数据库
- `api` 是唯一的权限守门，`rag` 不做用户级权限判断
- 上传文件存 `data/uploads/`，不进入仓库，文件名按 hash 重写避免目录穿越
- 文档原文与 chunk 内容默认不输出到日志

### 模型与 prompt

- 模型 API key 仅存在于 `.env` / 环境变量，`.env.example` 必须脱敏
- prompt 中包含的引用片段必须按权限过滤，禁止把无权访问的 chunk 进入 prompt
- 图片生成的 prompt 必须脱敏（去掉内部代号、人名、客户名）
- 任何外发 API 调用必须可在配置中关闭，便于离线部署评估

### 审计

- 所有写操作（消息编辑/删除、文档增删、知识卡审核、设置变更）写 `audit_logs`
- 高风险操作（删除、发布、降级）记录请求体摘要而非完整内容
- 审计日志只追加，不允许 UPDATE / DELETE

## 可观测性与运维

### 日志

- 统一通过 `requestLogger` 输出结构化日志（requestId + userId + path + duration + status）
- ERROR 级日志必须带 requestId 与上下文，便于跨服务串联
- 不在日志中打印用户原始 query 的完整内容（截断 + hash），避免敏感泄露

### 指标

- 至少暴露：QPS、P50/P95 延迟、错误率、LLM 调用次数与耗时、检索命中率、artifact 成功率
- 指标方案在「可观测性」阶段任务中具体选型（Prometheus / OpenTelemetry / 自研轻量方案）

### Trace

- 跨服务请求必须传递 requestId（已有 `requestContext`）
- 长链路（chat → rag → llm → embedding → diagram）必须能从一个 requestId 拉出完整时间线

### 备份与回滚

- SQLite 数据文件每日打包归档到 `data/backups/`，保留 30 天
- ChromaDB 向量库每周冷备一次
- 数据库迁移必须有回滚 SQL 或可重建脚本
- 模型/prompt 变更必须先在 `rag/evals/` 跑一遍，evals 不退化才允许上线

## 数据治理

- 文档冲突（同一主题多版本）必须显式标注版本与生效日期，检索时优先返回最新版本，回答时提示其它版本存在
- 过期文档支持人工标记 `deprecated`，标记后不再进入新检索（但历史回答的引用仍可定位）
- 错误知识纠错通道：用户点👎 + 说明 → 进入治理视图 → 触发知识卡/文档复审
- 知识卡与文档之间必须有反向引用，文档下线时提示影响范围
- 不同业务域（工艺/质量/设备/安全）通过 tag 而非目录隔离，避免目录结构与权限耦合

## 任务队列

### Web

- [x] `web`: 改造引用查看方式，将回答内引用点击打开回答作用域的证据详情，而不是把右侧作为全量引用仓库。
- [x] `web`: 验证问答卡片动作语义。用户问题 hover 显示复制、编辑、删除；删除必须二次确认，并连带删除对应回答或后续分支。回答不提供删除，提供复制、点赞、点踩、收藏、重试这些功能是否完善。
- [x] `web`: 输入框发送按钮在生成中切换为中断按钮，需验证中断后不会生成可收藏/可反馈的临时消息。
- [x] `web`: 修复发送消息和点击重试时聊天标题、输入框、消息列表闪跳或布局跳动；新增消息应无感插入，流式结束只静默合并后端 ID。
- [x] `web`: 将右侧栏接入真实当前线程目录、近期证据、笔记和结构化输出数据；现有导航壳已完成，但还需要和 API / RAG 同步成真实可用能力。
- [x] `web`: 将统一文档预览接入真实 PDF、HTML、代码块预览；现有预览壳已完成，但还需要和 API / RAG 的来源格式、文件流、清洗规则同步。
- [x] `web` / `api` / `rag`: 重新打开并补强 AI 整理图谱能力。完成标准为：
  - RAG 从回答正文、引用片段、章节标题、文档标题提取高频关键词、工艺对象、动作词、风险词、参数词。
  - RAG 为思维导图生成 root/category/keyword/evidence 节点和 contains/supported_by 边。
  - RAG 为流程图生成 step/decision/action 节点和 sequence/condition 边。
  - API 生成图谱时必须把回答正文和引用证据一起传给 RAG。
  - 图谱入口放在每条回答卡片结束后的下方，不放在右侧栏。
  - 图谱用弹窗展示，避免挤压聊天阅读区和右侧导航。
  - 前端只消费 RAG 返回的节点、边、布局和渲染元数据做通用 SVG 渲染；图结构、分类、证据挂载、布局规则和节点样式规则不写死在前端。
- [x] `web`: 做一次聊天端到端回归，覆盖登录、发送、中断、切换会话、滚动恢复、引用详情、复制、删除确认、点赞/点踩、收藏、重试，确认 UX/UI 不闪跳。

### API

- [x] `api`: Review 并加固消息编辑/删除的分支截断语义，确认删除用户问题会删除对应回答和后续分支，并保持权限校验。
- [x] `api`: 为消息删除/编辑补充接口烟雾或集成测试，覆盖非本人会话、缺失消息、正常删除、正常编辑。
- [x] `api`: 梳理现有 `message_sources` 和文档接口是否足够支撑 source detail API；如果足够，定义只读详情接口；如果不足，在 `progress.txt` 记录缺失字段。
- [x] `api`: 为文档预览能力明确 MIME/文件类型返回契约，优先覆盖 Markdown、纯文本、PDF、HTML、代码块来源，不改变 RAG 检索逻辑。
- [x] `api`: 评估笔记能力的数据归属，确定会话笔记、回答笔记、引用笔记是否需要独立表；本轮只产出 API 契约或最小后端切片。

### RAG

- [x] `rag`: 扩展 source metadata 设计，明确 document、section、chunk、page、offset、format、snippet 的可用性和兼容策略。
- [x] `rag`: 在不破坏现有 API consumers 的前提下，为检索结果补充可用于 source detail 的上下文字段。
- [x] `rag`: 为 Markdown、PDF、HTML、代码块等内容格式记录来源格式信息，供后续预览层使用。
- [x] `rag`: 将思维导图/流程图从 placeholder 升级为基于关键词、步骤、条件词、引用证据生成结构化 Diagram IR。

### 仓库与 Ralph

- [x] `chore`: 初始化 Ralph 控制文件和一次一任务的执行规则。
- [x] `chore`: 将 Ralph runner 固定为 Codex，避免本地 Claude Code `/v1/messages` 403 阻塞任务。
- [x] `chore`: 修复根 `pnpm dev` workspace 编排，避免 `dev:web` 被当作子包脚本解析。
- [x] `chore`: 修复 `pnpm dev:rag`，改用 `rag/.venv/bin/python` 启动 uvicorn。
- [x] `chore`: 将当前 PRD/progress 按 Ralph 要求重整为可执行任务源。

## 推荐执行顺序

当前聊天体验第一阶段任务已基本收口。下一阶段进入“企业级文档知识库智能工作台”升级，详细方案见 [企业级文档知识库智能工作台升级方案](docs/企业级文档知识库智能工作台升级方案.md)。

企业级知识工作台阶段已完成。后续进入“知识资产沉淀层”升级：继续以 RAG 精准问答、证据追溯、权限和拒答为主线，吸收 LLM Wiki 的主题页、术语库、FAQ、关系网络和人工审核发布能力，让高价值问答沉淀为可浏览、可复用、可治理的企业知识资产。

历史推荐顺序：

1. `web`: 右侧栏瘦身为 `路线图 / 知识笔记` 两个 tab；路线图基于当前 messages 生成，节点可点击定位回答，知识笔记沿用现有 chat_notes。
2. `api`: 新增 `chat_artifacts` 持久化契约，让图解从临时按钮结果升级为回答产物，刷新页面后仍可恢复。
3. `web` / `rag`: 引入 Excalidraw 图解渲染，替换手写 SVG；RAG 输出 Excalidraw scene 或兼容转换结构，前端只负责渲染。
4. `rag` / `api`: 增加 Visual Planner，在回答完成后自动判断是否需要流程图、架构图、思维导图、表格或图片 artifact。
5. `api` / `web`: 增强知识笔记聚合，将回答笔记、引用评论、风险提示和人工补充按回答/source 汇总。
6. `api` / `rag` / `web`: 受控接入图片生成能力，只在用户明确要求或培训/示意场景中生成 image artifact。

下一阶段推荐顺序：

1. `api` / `rag`: 建立术语库契约，支持缩写、别名、定义、来源文档、关联主题和检索扩展词。
2. `api`: 新增知识卡片数据模型，支持 AI 草稿、人工审核、发布、归档和来源引用。
3. `rag` / `api`: 从高置信回答、引用和知识笔记中生成知识卡草稿，不把低证据回答沉淀为知识资产。
4. `api` / `web`: 增加 FAQ 沉淀能力，将高频问题、优质回答、引用依据和适用范围组织为可复用条目。
5. `rag` / `api`: 建立轻量知识关系网络，连接主题、设备、参数、风险、流程、异常和处理方法。
6. `web`: 新增知识资产浏览入口，支持术语、知识卡、FAQ、相关主题和反向引用浏览。
7. `api` / `web`: 增加人工审核发布工作流，工程师可修订 AI 草稿并发布为可信知识。
8. `rag` / `api`: 让术语库、知识卡和 FAQ 反哺检索与回答，提升召回、改写和图解稳定性。

## 下一阶段任务队列

### 结构化精准问答与图解

- [x] `rag`: 定义 `AnswerIR` Pydantic schema，覆盖回答状态、claims、citations、query_rewrite、confidence、warnings，并保持现有 ChatResult 兼容。
- [x] `rag`: 增强多轮问题重写能力，输出可追踪的重写结果，覆盖指代、省略、无歧义不改写和低信息输入。
- [x] `api`: 将原始问题、重写问题、AnswerIR 摘要和引用覆盖信息持久化到回答 metadata，供前端和调试面板读取。
- [x] `rag`: 增加拒答与低证据策略，低信息输入、无有效引用、证据不足或上下文冲突时返回结构化 `insufficient_context`。
- [x] `rag`: 定义新版 `DiagramIR` schema 和校验器，支持 `can_generate`、节点/边合法性、引用覆盖、布局建议和质量告警。
- [x] `rag`: 将图解生成从关键词规则升级为 LLM structured output，要求模型基于回答和引用提炼精简节点，不把证据节点画进图。
- [x] `api`: 改造 artifact 质量门槛和置信度，使用 DiagramIR 质量分与校验结果决定是否保存或展示图解。
- [x] `web`: 收敛图解前端为纯渲染层，只做 DiagramIR 合法性检查、布局渲染和体验兜底，不再写业务提取规则。
- [x] `rag`: 建立精准问答、多轮重写和图解质量评估集，覆盖参数问答、流程问答、异常排查、拒答、指代追问和图解拓扑。

### 企业级知识工作台

- [x] `web`: 将右侧栏瘦身为 `路线图 / 知识笔记` 两个 tab。路线图展示当前会话关键问答节点、总结精髓、证据/artifact 状态，并支持点击定位到对应回答；知识笔记展示现有 session/message/source 笔记。
- [x] `api`: 新增 `chat_artifacts` 数据表和基础接口，支持按 message 保存、读取、重新生成和软删除 artifact；消息返回时携带 artifacts。
- [x] `web`: 新增通用 `ArtifactCard` / `ArtifactModal` 渲染层，将回答中的 artifacts 直接显示在对应回答下方，而不是放在右侧栏或只通过按钮触发。
- [x] `rag`: 将现有 Diagram IR 升级为可输出 Excalidraw scene 的 artifact payload，保留来源证据、生成理由和置信度。
- [x] `web`: 引入 `@excalidraw/excalidraw`，用只读白板图解替换当前手写 SVG 图谱弹窗。
- [x] `rag` / `api`: 增加 Visual Planner，基于问题、回答、引用和置信度判断是否自动生成流程图、架构图、思维导图、表格或图片 artifact。
- [x] `api` / `web`: 增强知识笔记聚合视图，将回答笔记、引用评论、风险提示和人工补充按回答与 source 组织。
- [x] `api` / `rag`: 设计受控 image artifact 生成链路，包含 prompt 脱敏、权限继承、异步生成和失败兜底；本阶段只做契约，不默认自动生成图片。

### LLM Wiki 式知识资产层

定位：本阶段不是把系统改成 Wiki，而是在企业级 RAG 知识库上增加可治理的知识沉淀层。RAG 继续负责精准问答、引用证据、拒答和权限；知识资产层负责把高价值回答、术语、FAQ、主题卡和关系网络沉淀为长期可复用内容。

- [x] `api` / `rag`: 建立术语库契约，支持术语、缩写、别名、定义、适用场景、来源文档、关联主题和检索扩展词；首批覆盖 OCV、DCR、EOL、SOC、SOP、CCD、Busbar 等电池产线常见术语。
- [x] `rag`: 在查询重写和检索前使用术语库扩展别名、缩写和同义表达，并记录扩展命中信息，避免把术语扩展做成不可追踪的黑盒。
- [x] `api`: 新增知识卡片数据模型，字段包含主题、摘要、关键参数、步骤、风险、处理方法、引用来源、关联术语、状态、审核人和版本历史。
- [x] `rag` / `api`: 从高置信回答、引用片段、知识笔记和图解 artifact 中生成知识卡草稿；低置信、拒答、无引用或证据冲突回答不得进入知识卡草稿。
- [x] `api` / `web`: 增加知识卡人工审核发布流，支持 AI 草稿、人工修订、发布、退回、归档和版本对比；发布前必须保留引用证据。
- [x] `api` / `web`: 增加 FAQ 沉淀能力，将高频问题、优质回答、引用证据、适用范围、失效条件和关联知识卡组织为可复用条目。
- [x] `rag` / `api`: 建立轻量知识关系网络，连接主题、设备、参数、风险、流程、异常、处理方法、文档、FAQ 和知识卡，供相关知识推荐与图解生成复用。
- [x] `web`: 新增知识资产浏览入口，支持术语库、知识卡、FAQ、相关主题、反向引用和来源文档浏览；保持对话问答仍为主入口。
- [x] `api` / `web`: 在回答页和知识笔记聚合页展示“可沉淀为知识卡 / FAQ”的入口，并明确 AI 草稿、待审核、已发布状态。
- [x] `rag` / `api`: 让已发布术语、知识卡和 FAQ 反哺 RAG 检索、问题改写、置信度判断和图解规划，但必须在回答 metadata 中标记使用了哪些知识资产。
- [x] `rag`: 扩展质量评估集，覆盖术语召回、知识卡草稿生成、FAQ 复用、关系推荐、人工发布后反哺检索，以及错误知识不得沉淀。
- [x] `api` / `web`: 增加知识资产治理视图，展示待审核草稿、低证据草稿、过期来源、被频繁引用的知识卡和需要人工复核的风险项。

### 架构治理与代码债

呼应 [项目架构总览](docs/项目架构总览.md) §6/§7 列出的架构债，优先做不改行为的整理。

- [x] `rag`: 将 `rag/app/evaluation/` 重命名为 `rag/app/artifacts/`，同步修正所有 import；在 `rag/app/artifacts/README.md` 写清楚和 `rag/evals/` 的边界。命名错位会让新人持续找错地方加代码。
- [x] `web`: 清理 `web/src/**/*.js` 死代码（与 `.tsx` 同名共存的 56 个 `.js`），先确认 `tsconfig.json` 输出目录不是 `src/`，再批量删除并跑 `pnpm build:web` 验证。
- [x] `repo`: 修正 README/package.json/`.env.example` 三处 RAG 端口口径（统一为 8000 或 8001，二选一），并在 README 注明启动后健康检查地址。
- [x] `repo`: 将根目录 `人类阅读的/` 重命名为 `docs/参考资料/`，并把根目录 `企业知识库RAG精准问答+多轮会话记忆 全套落地流程方案.md` 移入；根目录只保留 README/AGENTS/CLAUDE/PRD/progress。
- [x] `api`: 拆分 `api/src/routes/chat.ts`（1433 行）为 `routes/chat/{sessions,messages,notes,sources,artifacts}.ts`，`index.ts` 只做装配；行为不变，跑 `api/tests/run-tests.sh` 验证。
- [x] `rag`: 拆分 `rag/app/core/pipeline.py`（899 行）为 `pipeline/{rewrite,retrieve,generate,refusal}.py`，`__init__.py` 顺序编排；`rag/evals/` 全量回归。
- [x] `api`: 拆分 `api/src/db/index.ts`（555 行）按主题分文件（identity / documents / chat / artifacts / knowledge / platform），`index.ts` 只做 `initDb` 装配。
- [x] `repo`: 跨服务统一 artifact 命名：DB 字段、API 响应、前端类型、RAG 输出统一用 `artifact` + `type`（diagram/flowchart/mindmap/chart/table/image），消除"artifact / diagram / chart"混用。
- [x] `api`: 在 `services/ragClient.ts` 上加 zod 校验，让 RAG 返回的 `AnswerIR` / `DiagramIR` 在边界爆出契约破坏，而不是渗到前端。
- [x] `web`: 把 `pages/ChatPage.tsx` + `components/chat/*` + 相关 hooks/types 收敛到 `features/chat/`，其它页面同样改造；当前 `components/*` 按 UI 类型分类已撑不住。
- [x] `docs`: 用 mermaid 或 Excalidraw 画一份运行时拓扑 + 数据流图放在 `docs/图/`，作为「项目架构总览」的可视化补充。

### 性能、安全与可观测性

把 §质量目标 / §性能预算 / §安全与权限 / §可观测性 落成具体能力。

- [x] `api` / `rag`: 在 `requestLogger` 基础上加结构化字段（requestId、userId、route、duration_ms、status、error_code），并在两端贯通同一 requestId。
- [ ] `rag`: 给 `core/pipeline.py` 各阶段（rewrite/retrieve/refusal/generate/artifact）打点耗时，输出到日志和未来的指标系统。
- [ ] `api`: 暴露 `/api/admin/metrics`（仅 system_admin），输出 QPS、P50/P95、错误率、LLM 调用次数、检索命中率、artifact 成功率的滚动窗口统计。
- [ ] `repo`: 选型并接入轻量指标方案（首选 Prometheus pull + Grafana，或自研 JSON dump）；选型决定写入 `docs/可观测性方案.md`。
- [ ] `api`: 为高风险接口（消息删除、文档删除、知识卡发布、设置变更）补全 `audit_logs` 写入，并在治理视图中暴露。
- [ ] `api`: 实现 SQLite 日备 + ChromaDB 周备脚本，放到 `scripts/backup_*.sh`，并在 README 写明恢复流程。
- [ ] `rag`: 接入 `usage_guard` 的硬上限（按用户/按时段），超限返回结构化错误而不是 502。
- [ ] `repo`: `.env.example` 增补所有可关闭的外发开关（LLM_PROVIDER / IMAGE_GEN_ENABLED / TELEMETRY_ENABLED），便于离线部署。
- [ ] `api`: 在 `extractUser` 中关闭开发模式的 `system_admin` 默认值（NODE_ENV=production 时强制 401），避免线上裸奔。
- [ ] `rag`: 文档原文与 chunk 内容默认不进日志，确认 `requestLogger` 与 `pipeline` 的所有 print/log 都做了截断或 hash。

### 企业系统集成

让知识库成为企业内可被调用的能力，而不是孤岛。本阶段不做泛化 SDK，只覆盖企业内合规接入。

- [ ] `api`: 暴露面向受控集成的 OpenAPI（`/api/integration/v1/*`），覆盖检索、问答、知识卡查询；走独立 API Token + 速率限制，与用户会话隔离。
- [ ] `api`: 增加 webhook 出站能力（知识卡发布、文档冲突、高频拒答），可向企业内 IM / 工单系统通知；地址与签名通过 `settings` 表配置。
- [ ] `api` / `rag`: 设计 MES/PLM/ERP 离线导出文件的批量导入通道（CSV/JSON/PDF 包），不接入实时数据流。
- [ ] `web`: 提供"嵌入式问答控件"打包产物（iframe 或 Web Component），便于内嵌到 MES 工位看板，保持鉴权一致。
- [ ] `api`: 对外接口必须经独立鉴权链（API Token），不复用 web 会话 cookie；token 可吊销、可审计。

## 每轮验收模板

阶段完成、阻塞、范围变化、用户要求或 AFK loop 结束时，在 `progress.txt` 追加：

```text
## YYYY-MM-DD - <任务名>

- Service: web/api/rag/chore
- Task: <PRD 中的任务原文>
- Changed: <本轮实际改了什么>
- Verification:
  - <命令或浏览器路径>
- Commit: <hash 或未提交原因>
- Blockers: <无 / 阻塞项>
- Next: <建议下一轮任务>
```

## 完成标准

- 所有任务完成，或明确标记为暂不做并写明原因。
- 每个完成任务都有可追溯 commit。
- `progress.txt` 能让新上下文的 Codex 直接接着执行。
- 主聊天流程在浏览器中验证可用：登录、提问、流式生成、中断、滚动、切换会话、引用查看、消息动作。
- 不存在无关主题混杂 commit。

## 产品级 Definition of Done

任务级 DoD 见「每轮验收模板」与 [项目执行规则清单](docs/EXECUTION_RULES.md)。下面是**阶段/版本**级别的产品 DoD，用于回答"这个阶段算不算交付完了"。

### 功能 DoD

- 关键链路（登录、提问、流式、中断、重试、引用、artifact、笔记、知识卡发布）在浏览器中端到端跑通
- 新增接口必须有 `api/tests/*.sh` 或等价 smoke 覆盖
- 新增 RAG 能力必须在 `rag/evals/` 至少有一个用例
- 新增数据表必须可通过删库重建脚本恢复

### 质量 DoD

- `rag/evals/` 全量回归未退化（或对退化项有明确说明与补偿措施）
- 高风险路径手测覆盖（参见 [EXECUTION_RULES](docs/EXECUTION_RULES.md) §1.1）
- 拒答策略在低证据样本上保持 ≥ 0.95 拒答正确率
- 无新增 P0/P1 缺陷，已知 P2 写入 issue 列表

### 性能 DoD

- 首 token 延迟 P95 不退化超过 15%
- 检索 P95 ≤ 800ms
- 新增同步链路对端到端 P95 影响 ≤ 1s，超过必须改异步

### 安全 DoD

- 新增写接口已挂 `requireAuth` + `requirePermission`
- 新增写操作已写 `audit_logs`
- 没有把 chunk 内容、用户 query 全文打到日志或外发请求里
- 模型 key、企业内部 token 没有进入仓库

### 文档 DoD

- 用户/运维侧的变化更新到 `README.md` 或 `docs/` 对应文档
- 架构层面的变化更新到 [项目架构总览](docs/项目架构总览.md)
- 数据模型变化更新到 [项目架构与边界标准](docs/架构标准.md) 或本 PRD
- PRD 任务勾选 + commit message 能让新上下文复现进度

## 里程碑

里程碑按"用户能拿到什么"切分，不按代码行数。日期是目标日期不是承诺日期；范围调整必须更新本节并在 `progress.txt` 记录原因。

### M1 — 精准问答可用（已完成）

- 三服务跑通、登录、流式问答、引用展示、消息动作、文档导入
- `AnswerIR` + 拒答策略上线
- 产物：当前主分支

### M2 — 知识工作台（已完成）

- 右侧栏瘦身为路线图 + 知识笔记
- `chat_artifacts` 持久化 + Excalidraw 渲染 + Visual Planner
- 知识笔记聚合视图
- 产物：当前主分支

### M3 — 知识资产层（已完成）

- 术语库、知识卡、FAQ、知识图谱、人审发布流、治理视图
- 已发布资产反哺 RAG 检索
- 产物：当前主分支

### M4 — 架构治理与可运维性（规划中）

目标：把现有能力的可维护性和可运维性补齐，不引入新的产品能力。

- 完成「架构治理与代码债」全部任务
- 完成「性能、安全与可观测性」全部任务
- 产出：可在生产环境上线的最小可运维版本

### M5 — 企业系统集成（规划中）

目标：让知识库成为企业内可被调用的能力。

- 完成「企业系统集成」全部任务
- 至少接入 1 个真实企业内系统（MES 工位看板或工单系统）作为试点
- 产出：可对外的 OpenAPI + 嵌入式控件 + 试点案例

### M6 — 持续学习与运营（待评估）

候选方向（择一或择二，不全做）：

- 主动学习：基于用户反馈与拒答日志，自动推荐文档补充清单
- 个性化：按用户角色调整回答风格与默认 artifact 类型
- 多模态：受控接入图片/视频内容理解（仅工艺图、设备图）

## 风险与依赖

| 风险 | 影响 | 应对 |
|------|------|------|
| LLM 服务商可用性 / 限流 / 涨价 | 主链路不可用 | `usage_guard` 兜底、支持多 provider 切换、离线模式可降级到纯检索 |
| 文档质量参差（OCR 错、版本混乱、口语化 SOP） | 检索准确率塌方 | 数据治理任务、版本/失效标记、过期下线、人工复审 |
| 知识卡审核人力不足 | 沉淀链路堵塞 | 治理视图按优先级排序、可批量审核、不强制全部入库 |
| 误拒答（应该能答却拒答） | 用户失去信任 | evals 同时跑回答能力和拒答能力，二者不能单边优化 |
| 内部知识泄露给外部 LLM | 合规与安全事故 | prompt 脱敏、外发开关、关键文档可标记"禁外发"、离线模型选项 |
| 巨石文件继续生长 | 维护成本指数上升 | M4 里程碑硬切，拆完之前不再新增同类堆积 |
| 与 MES/PLM/ERP 接入边界不清 | 沦为定制项目 | 只接收离线导出，不做实时集成；接口走独立鉴权链 |

外部依赖：

- DeepSeek API（或同等 LLM provider）
- ChromaDB（本地，可替换为同等向量库）
- SQLite（本地，单机部署足够；多机时再评估 Postgres）
- pnpm workspace + Vite + Express + FastAPI（基础设施稳定，无替换计划）

## 文档与本 PRD 的关系

- 本 PRD 是**任务源 + 阶段目标 + 产品 DoD**。每次产品方向变化先更新本 PRD。
- [项目架构总览](docs/项目架构总览.md) 是**现状盘点 + 架构债清单 + 优化建议**。每次架构变化更新它。
- [架构标准](docs/架构标准.md) 是**服务边界硬规则**。变更服务职责才更新。
- [EXECUTION_RULES](docs/EXECUTION_RULES.md) 是**执行细则唯一来源**。变更工作流才更新。
- [企业级文档知识库智能工作台升级方案](docs/企业级文档知识库智能工作台升级方案.md) 是**产品演进方案**，对应 M2/M3 已落地，可作为历史决策记录保留。

发生冲突时优先级：EXECUTION_RULES > 架构标准 > PRD > 其它文档。
