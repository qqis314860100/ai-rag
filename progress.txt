# Ralph 进度记录

## 当前状态

- Ralph 已固定为 Codex runner，不再依赖本地 Claude Code。
- 当前 PRD 已按 Ralph 要求重整为单任务、单服务、可验证、可提交的任务队列。
- 最新完成 `web`: 将右侧栏改造成当前会话导航壳，包含线程目录、近期证据、笔记占位和结构化输出入口。
- 最新完成 `web`: 建立统一文档预览壳，支持文本 / Markdown / 原文切换，并为 PDF / HTML / 代码块保留禁用状态。
- 最新完成 `web`: 增加 AI 整理入口占位，思维导图 / 流程图只触发本地待接入提示，不调用后端 API。
- 最新完成 `web`: 聊天端到端回归，覆盖登录、发送、中断、切换会话、滚动恢复、引用详情、复制、删除确认、点赞/点踩、收藏、重试。
- 根 `pnpm dev` 和 `pnpm dev:rag` 启动脚本已修复；`pnpm dev:rag` 依赖 `rag/.venv` 已存在。
- 本轮 `web` 回归已完成；记录随本轮提交保存。
- 最新完成 `api`: 加固消息编辑/删除的分支截断语义，用户消息编辑/删除会按稳定消息顺序截断后续分支，并拒绝直接编辑/删除 assistant 消息。
- 下一轮建议优先处理 `api`: 为消息删除/编辑补充接口烟雾或集成测试，覆盖非本人会话、缺失消息、正常删除、正常编辑。

## 已完成

### 2026-05-24 - 初始化 Ralph loop

- Service: chore
- Task: 初始化 Ralph 控制文件和一次一任务的执行规则。
- Changed: 添加 `PRD.md`、`progress.txt`、`ralph-once.sh`、`afk-ralph.sh`。
- Verification:
  - 脚本可读，任务规则与仓库执行规则一致。
- Commit: `cef298d`
- Blockers: 无。
- Next: 执行第一个 web 任务。

### 2026-05-24 - 改造回答引用详情

- Service: web
- Task: `web`: 改造引用查看方式，将回答内引用点击打开回答作用域的证据详情，而不是把右侧作为全量引用仓库。
- Changed: 将聊天引用面板从全量来源列表调整为回答作用域证据详情；引用点击打开证据详情，可复制、追问、打开原文预览。
- Verification:
  - `pnpm run lint:web` 通过，但存在既有无关 warning。
  - `pnpm run build:web` 通过。
  - 浏览器烟雾验证登录和聊天页渲染。
- Commit: `49d1a39`
- Blockers: 当前种子/活跃会话没有真实 sources，未能在浏览器中完整打开真实证据详情。
- Next: 做滚动体验修复。

### 2026-05-24 - 记录引用详情迭代

- Service: chore
- Task: 记录 Ralph 迭代结果。
- Changed: 更新 Ralph 进度记录。
- Verification:
  - `git show --stat 49d1a39`
- Commit: `ba87c41`
- Blockers: 无。
- Next: 修复 Ralph runner。

### 2026-05-24 - 固定 Codex Ralph runner

- Service: chore
- Task: 将 Ralph runner 固定为 Codex，避免本地 Claude Code `/v1/messages` 403 阻塞任务。
- Changed: `ralph-once.sh` 和 `afk-ralph.sh` 改为只调用 `codex exec`；本地 `ralph-loop` 插件只保留 Codex marketplace 和 `.codex-plugin`，删除 Claude 插件入口。
- Verification:
  - `bash -n ralph-once.sh afk-ralph.sh`
  - `python3 -m py_compile /Users/tomtong/.codex/plugins/marketplaces/ralph-loop/plugins/ralph-loop/scripts/init_ralph_loop.py`
  - `validate_plugin.py /Users/tomtong/.codex/plugins/cache/ralph-loop/ralph-loop/0.1.0`
  - `codex plugin list --marketplace ralph-loop`
- Commit: `e380352`
- Blockers: 无。
- Next: 重整 PRD，使后续 Codex Ralph 可以稳定选择任务。

### 2026-05-24 - 验证问答卡片动作语义

- Service: web
- Task: `web`: 验证问答卡片动作语义。用户问题 hover 显示复制、编辑、删除；删除必须二次确认，并连带删除对应回答或后续分支。回答不提供删除，提供复制、点赞、点踩、收藏、重试这些功能是否完善。
- Changed: 回答重试改为按当前回答定位上一条用户问题并从该分支重新生成；用户编辑改为先通过现有删除/截断接口移除旧分支再重新提问；流式或未持久化用户消息禁用编辑/删除；流式完成后重新拉取会话消息，确保后续动作使用后端持久化消息 ID。
- Verification:
  - `pnpm run lint:web` 通过，仍有既有无关 warning。
  - `pnpm run build:web` 通过。
  - Chrome `http://localhost:5174/chat` 烟雾验证：聊天页渲染，回答动作包含复制/收藏/点赞/点踩/重新生成此回答/查看证据且不含删除；用户问题动作包含复制/编辑/删除；编辑模式可打开并取消；删除二次确认文案正确并已取消，未删除数据。
- Commit: `ff85043`
- Blockers: 无。注意：根脚本 `pnpm dev` 当前会因 workspace 脚本解析失败退出，本轮改用 `pnpm run dev:web`；API 端口 3001 已有进程占用，浏览器烟雾复用现有 API。
- Next: `web`: 输入框发送按钮在生成中切换为中断按钮，需验证中断后不会生成可收藏/可反馈的临时消息。

### 2026-05-24 - 验证中断态消息动作

- Service: web
- Task: `web`: 输入框发送按钮在生成中切换为中断按钮，需验证中断后不会生成可收藏/可反馈的临时消息。
- Changed: 未改业务代码；确认现有实现已满足要求，中断时会保留可见的临时回答但不暴露收藏、点赞、点踩或证据查看动作。
- Verification:
  - `pnpm run lint:web`
  - `pnpm run build:web`
  - 浏览器验证：发送问题后按钮切换为“中断生成”；在生成中点击中断后，半截临时回答仅保留复制/重新生成，不出现收藏/点赞/点踩/查看证据。
- Commit: `af4b255`
- Blockers: 无。
- Next: `web`: 将右侧栏改造成当前会话导航壳，包括当前线程目录、近期证据、笔记占位、结构化输出入口，不依赖新增后端 API。

### 2026-05-24 - 修复 workspace 启动配置

- Service: chore
- Task: 修复根 `pnpm dev` workspace 编排，避免 `dev:web` 被当作子包脚本解析。
- Changed: 根 `dev` 脚本改为 `pnpm --parallel --filter web --filter api dev`；删除 `.npmrc` 中 npm 不识别的 `onlyBuiltDependencies` 重复配置；记录 `.ralph-loop.last.md`。
- Verification:
  - `npm run dev` 不再报 `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT`。
  - npm 不再输出 `Unknown project config "onlyBuiltDependencies"` 警告。
- Commit: `a59e8ca`
- Blockers: 该提交为根级配置提交，不属于单一 `web/api/rag` 服务 commit；后续如需严格回滚，应按 root chore 单独处理。
- Next: 修复聊天发送/重试引发的可见闪跳。

### 2026-05-24 - 修复聊天发送和重试闪跳

- Service: web
- Task: `web`: 修复发送消息和点击重试时聊天标题、输入框、消息列表闪跳或布局跳动；新增消息应无感插入，流式结束只静默合并后端 ID。
- Changed:
  - 流式完成后从整组消息替换改为静默合并后端持久化 ID，保留本地 DOM key。
  - 新会话第一次发送时跳过自动空消息加载，避免乐观消息被覆盖。
  - 头部 `生成中...` 改为常驻占位并只切透明度，避免标题被挤动。
  - 发送后 textarea 保持发送前高度，生成中使用 `readOnly` 而不是 `disabled`。
  - 去掉消息逐条 `fadeInUp` 位移动画，滚动到底部改为更轻的按帧跟随，并关闭 `.chat-scroll-area` 的 `overflow-anchor`。
- Verification:
  - `pnpm run lint:web` 通过，仍有既有无关 warning。
  - `pnpm run build:web` 通过。
  - 浏览器几何探针：发送前、发送中、完成后，输入区 `top/height`、滚动区高度、标题位置保持一致。
- Commit: `10c906f`
- Blockers: 浏览器验证中的 `/api/chat` 流式响应使用 Playwright route mock；真实 RAG 端到端流式回归仍需在后续专门执行。
- Next: `web`: 将右侧栏改造成当前会话导航壳；随后做聊天端到端回归。

### 2026-05-24 - 修复 RAG 本地启动命令

- Service: chore
- Task: 修复 `pnpm dev:rag` 本地启动命令。
- Changed: 根 `dev:rag` 脚本改为使用 `rag/.venv/bin/python -m uvicorn`，避免当前 macOS shell 中 `python` 不存在或未安装 uvicorn 的问题。
- Verification:
  - 手动用 `cd rag && .venv/bin/python -m uvicorn app.main:app --reload --port 8000` 启动通过，健康检查请求成功。
- Commit: `eb7b14f`
- Blockers: commit message 未使用推荐 scope；后续提交应恢复 Conventional Commits 规范。
- Next: 更新 PRD/progress，使下一轮任务从真实状态继续。

### 2026-05-24 - 改造右侧会话导航壳

- Service: web
- Task: `web`: 将右侧栏改造成当前会话导航壳，包括当前线程目录、近期证据、笔记占位、结构化输出入口，不依赖新增后端 API。
- Changed: 新增 `ConversationNavigator`，将右侧栏从仅在引用存在时出现的来源面板改为可独立开关的会话导航壳；保留回答作用域证据详情并嵌入“证据”视图；为消息节点补充稳定 DOM 锚点，支持从线程目录跳转；移动端以右侧抽屉方式打开，不挤压主聊天区。
- Verification:
  - `pnpm run lint:web` 通过，仍有既有无关 warning。
  - `pnpm run build:web` 通过。
  - Playwright Chromium `http://localhost:5173/chat` 桌面烟雾验证：会话导航、当前线程目录、近期证据、会话笔记、结构化输出均可见；右侧栏可关闭再打开；头部和输入框 `x/y/height` 无变化。
  - Playwright Chromium `390x844` 移动端烟雾验证：右侧导航可作为 336px 抽屉打开，遮罩关闭有效。
- Commit: `40ccd00`
- Blockers: Ralph 自动执行时浏览器 MCP 被已有实例占用，已中断并改用独立 Playwright Chromium 完成验证。
- Next: `web`: 做一次聊天端到端回归，覆盖登录、发送、中断、切换会话、滚动恢复、引用详情、复制、删除确认、点赞/点踩、收藏、重试，确认 UX/UI 不闪跳。

### 2026-05-24 - 建立统一文档预览壳

- Service: web
- Task: `web`: 建立统一文档预览壳，先支持现有文本/Markdown/原始字段展示，并为 PDF、HTML、代码块预览留清晰禁用状态。
- Changed: 重做 `DocPreview`，加入统一预览模式切换；支持文本 / Markdown / 原文三种可用视图；为 PDF / HTML / 代码块提供明确禁用态；并在切换源时重置预览状态，避免旧内容残留。
- Verification:
  - `pnpm run lint:web`
  - `pnpm run build:web`
  - Chrome `http://localhost:5173/chat` 烟雾验证：从右侧证据导航打开预览后，可切换文本 / Markdown / 原文，PDF / HTML / 代码块按钮保持禁用，空原文显示空态。
- Commit: `c3022bd`
- Blockers: 选中的样例证据 `raw` 接口返回空时会显示空态，属于当前后端数据现状。
- Next: `web`: 增加 AI 整理入口占位，提供“思维导图 / 流程图待接入”的本地提示，不调用不存在的后端 API。

### 2026-05-24 - 增加 AI 整理入口占位

- Service: web
- Task: `web`: 增加 AI 整理入口占位，可触发“思维导图/流程图待接入”的本地提示，不调用不存在的后端 API。
- Changed: 在会话导航中增加 AI 整理入口占位；保留思维导图和流程图快捷入口，点击只触发本地 Toast 提示，不接入任何后端请求。
- Verification:
  - `pnpm run lint:web` 通过，仍有既有无关 Fast Refresh warning。
  - `pnpm run build:web` 通过。
  - Playwright `http://127.0.0.1:5174/chat?session=smoke-session` 烟雾验证：AI 整理、思维导图、流程图入口均可见；点击后出现 `AI 整理待接入`、`思维导图待接入`、`流程图待接入` 本地提示；点击期间新增 `/api` 请求数为 0。
- Commit: `c520bcb`
- Blockers: 无。
- Next: `web`: 做一次聊天端到端回归，覆盖登录、发送、中断、切换会话、滚动恢复、引用详情、复制、删除确认、点赞/点踩、收藏、重试，确认 UX/UI 不闪跳。

### 2026-05-24 - 聊天端到端回归

- Service: web
- Task: `web`: 做一次聊天端到端回归，覆盖登录、发送、中断、切换会话、滚动恢复、引用详情、复制、删除确认、点赞/点踩、收藏、重试，确认 UX/UI 不闪跳。
- Changed: 未改业务代码；完成真实本地服务回归并将该 web 任务标记完成。
- Verification:
  - `pnpm run lint:web` 通过，仍有既有 Fast Refresh warning。
  - `pnpm run build:web` 通过。
  - `curl http://localhost:3001/api/admin/health` 返回 API/RAG/数据库均为 `ok`。
  - `curl http://localhost:8000/rag/health` 返回 RAG 服务、Chroma 和 LLM provider 均为 `ok`。
  - Playwright `http://localhost:5173/chat` 回归：登录、发送、真实 RAG 流式生成、中断、重试、切换会话、滚动恢复、引用详情、复制、删除确认、点赞/点踩、收藏、最终重新生成均通过。
  - Playwright 文档预览回归：文本 / Markdown / 原文切换可用，PDF / HTML / 代码块保持禁用占位；缺失 raw 内容时显示既有空态。
- Commit: 随本轮记录提交；最终 hash 见本轮执行结果。
- Blockers: 无。备注：浏览器 console 仅出现 favicon 404 和缺失 raw 文档内容 404，后者已由预览空态处理。
- Next: `api`: Review 并加固消息编辑/删除的分支截断语义，确认删除用户问题会删除对应回答和后续分支，并保持权限校验。

### 2026-05-24 - 加固消息分支截断语义

- Service: api
- Task: `api`: Review 并加固消息编辑/删除的分支截断语义，确认删除用户问题会删除对应回答和后续分支，并保持权限校验。
- Changed:
  - `chat_messages` 读取顺序与截断顺序统一为 `created_at ASC, rowid ASC`，避免同时间戳消息在读取和截断时顺序不一致。
  - 消息编辑/删除分支截断改为 SQLite transaction，避免只更新消息但未完整删除后续分支的中间状态。
  - `PATCH /api/chat/messages/:id` 和 `DELETE /api/chat/messages/:id` 只允许处理 `user` 消息，直接拒绝 assistant 消息编辑/删除，保留回答不可随意删除的语义。
  - 修复 `documents` 上传文件过滤回调的 TypeScript 签名，以解除 API build 阻塞。
- Verification:
  - `pnpm run build:api`
  - `pnpm run lint:api`
  - 临时 API 服务 `API_PORT=3101 DATABASE_URL=file:/tmp/ai-rag-api-smoke.db pnpm --dir api exec tsx src/server.ts`
  - 临时数据库 API 烟雾：用户消息编辑后仅保留编辑后的用户消息；删除用户消息后分支消息清空；assistant 消息 PATCH/DELETE 均返回 400 且消息仍保留。
- Commit: `6821b26`
- Blockers: `node api/dist/server.js` 仍存在既有 ESM 目录导入运行问题，构建产物无法直接作为烟雾服务启动；本轮运行时烟雾改用 API dev runner。
- Next: `api`: 为消息删除/编辑补充接口烟雾或集成测试，覆盖非本人会话、缺失消息、正常删除、正常编辑。

## 当前待提交

- 无。

## 下一轮建议

- Service: api
- Task: `api`: 为消息删除/编辑补充接口烟雾或集成测试，覆盖非本人会话、缺失消息、正常删除、正常编辑。

- Required verification:
  - `pnpm run build:api`
  - 相关 API 烟雾或集成测试，至少覆盖非本人会话、缺失消息、正常删除、正常编辑。

- Follow-up:
  - 下一轮完成后，再做 `api`: 梳理现有 `message_sources` 和文档接口是否足够支撑 source detail API。

## 执行提醒

- 每轮只做一个任务。
- 每轮只改一个服务。
- 验证通过后再提交。
- 如果工作区不干净，先停下来说明，不要让 Ralph 自动吞掉无关改动。
