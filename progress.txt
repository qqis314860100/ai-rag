# Ralph 进度记录

## 当前状态

- Ralph 已固定为 Codex runner，不再依赖本地 Claude Code。
- 当前 PRD 已按 Ralph 要求重整为单任务、单服务、可验证、可提交的任务队列。
- 最新完成 `web`: 验证并收紧问答卡片动作语义。
- 下一轮应优先处理 `web`: 输入框发送按钮在生成中切换为中断按钮，并验证中断后不会生成可收藏/可反馈的临时消息。

## 已完成

### 2026-05-24 - 初始化 Ralph loop

- Service: chore
- Task: 初始化 Ralph 控制文件和一次一任务的执行规则。
- Changed: 添加 `PRD.md`、`progress.txt`、`ralph-once.sh`、`afk-ralph.sh`。
- Verification:
  - 脚本可读，任务规则与仓库执行规则一致。
- Commit: `cef298d`
- Blockers: 无。
- Next: 执行第一个 web 任务。

### 2026-05-24 - 改造回答引用详情

- Service: web
- Task: `web`: 改造引用查看方式，将回答内引用点击打开回答作用域的证据详情，而不是把右侧作为全量引用仓库。
- Changed: 将聊天引用面板从全量来源列表调整为回答作用域证据详情；引用点击打开证据详情，可复制、追问、打开原文预览。
- Verification:
  - `pnpm run lint:web` 通过，但存在既有无关 warning。
  - `pnpm run build:web` 通过。
  - 浏览器烟雾验证登录和聊天页渲染。
- Commit: `49d1a39`
- Blockers: 当前种子/活跃会话没有真实 sources，未能在浏览器中完整打开真实证据详情。
- Next: 做滚动体验修复。

### 2026-05-24 - 记录引用详情迭代

- Service: chore
- Task: 记录 Ralph 迭代结果。
- Changed: 更新 Ralph 进度记录。
- Verification:
  - `git show --stat 49d1a39`
- Commit: `ba87c41`
- Blockers: 无。
- Next: 修复 Ralph runner。

### 2026-05-24 - 固定 Codex Ralph runner

- Service: chore
- Task: 将 Ralph runner 固定为 Codex，避免本地 Claude Code `/v1/messages` 403 阻塞任务。
- Changed: `ralph-once.sh` 和 `afk-ralph.sh` 改为只调用 `codex exec`；本地 `ralph-loop` 插件只保留 Codex marketplace 和 `.codex-plugin`，删除 Claude 插件入口。
- Verification:
  - `bash -n ralph-once.sh afk-ralph.sh`
  - `python3 -m py_compile /Users/tomtong/.codex/plugins/marketplaces/ralph-loop/plugins/ralph-loop/scripts/init_ralph_loop.py`
  - `validate_plugin.py /Users/tomtong/.codex/plugins/cache/ralph-loop/ralph-loop/0.1.0`
  - `codex plugin list --marketplace ralph-loop`
- Commit: `e380352`
- Blockers: 无。
- Next: 重整 PRD，使后续 Codex Ralph 可以稳定选择任务。

### 2026-05-24 - 验证问答卡片动作语义

- Service: web
- Task: `web`: 验证问答卡片动作语义。用户问题 hover 显示复制、编辑、删除；删除必须二次确认，并连带删除对应回答或后续分支。回答不提供删除，提供复制、点赞、点踩、收藏、重试这些功能是否完善。
- Changed: 回答重试改为按当前回答定位上一条用户问题并从该分支重新生成；用户编辑改为先通过现有删除/截断接口移除旧分支再重新提问；流式或未持久化用户消息禁用编辑/删除；流式完成后重新拉取会话消息，确保后续动作使用后端持久化消息 ID。
- Verification:
  - `pnpm run lint:web` 通过，仍有既有无关 warning。
  - `pnpm run build:web` 通过。
  - Chrome `http://localhost:5174/chat` 烟雾验证：聊天页渲染，回答动作包含复制/收藏/点赞/点踩/重新生成此回答/查看证据且不含删除；用户问题动作包含复制/编辑/删除；编辑模式可打开并取消；删除二次确认文案正确并已取消，未删除数据。
- Commit: `ff85043`
- Blockers: 无。注意：根脚本 `pnpm dev` 当前会因 workspace 脚本解析失败退出，本轮改用 `pnpm run dev:web`；API 端口 3001 已有进程占用，浏览器烟雾复用现有 API。
- Next: `web`: 输入框发送按钮在生成中切换为中断按钮，需验证中断后不会生成可收藏/可反馈的临时消息。

## 当前待提交

- 无服务代码待提交。
- 本文件包含本轮进度记录，未并入 `ff85043`，避免 web 服务 commit 混入根级进度文件。

## 下一轮建议

- Service: web
- Task: `web`: 输入框发送按钮在生成中切换为中断按钮，需验证中断后不会生成可收藏/可反馈的临时消息。

- Required verification:
  - `pnpm run lint:web`
  - `pnpm run build:web`
  - 浏览器验证：发送问题后按钮切换为中断；中断后临时回答不出现收藏/反馈动作；可继续重新提问。

## 执行提醒

- 每轮只做一个任务。
- 每轮只改一个服务。
- 验证通过后再提交。
- 如果工作区不干净，先停下来说明，不要让 Ralph 自动吞掉无关改动。
